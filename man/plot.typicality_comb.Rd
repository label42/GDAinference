% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.typicality_comb}
\alias{plot.typicality_comb}
\title{Plot a combinatorial typicality test}
\usage{
\method{plot}{typicality_comb}(x, axes = c(1, 2), ...)
}
\arguments{
\item{x}{A \code{typicality_comb} object created with \code{keep_geometry = TRUE}
(the default of \code{\link[=typicality_comb]{typicality_comb()}}).}

\item{axes}{Length-2 integer vector giving the two axes (columns of the
analysed coordinates) to display. Default \code{c(1, 2)}.}

\item{...}{Currently ignored.}
}
\value{
A \pkg{ggplot2} object (drawn when printed).
}
\description{
Displays, on a plane defined by two axes, the reference cloud with its mean
point G, the group mean point C, and the \eqn{(1-\alpha)} compatibility
region: the principal \eqn{d_\alpha}-ellipse of the reference cloud,
translated so as to be centred on C (Le Roux, Bienaise & Durand 2019,
Prop. 3.6). When the analysis is two-dimensional the ellipse is exact;
for higher-dimensional analyses it is the ellipse of the displayed plane.
}
\details{
Requires the \pkg{ggplot2} package.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
res <- typicality_comb(Target, Target_group)
plot(res)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=typicality_comb]{typicality_comb()}}
}
