% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.homogeneity}
\alias{plot.homogeneity}
\title{Plot a combinatorial homogeneity test}
\usage{
\method{plot}{homogeneity}(x, axes = c(1, 2), ...)
}
\arguments{
\item{x}{A \code{homogeneity} object created with \code{keep_geometry = TRUE} (the
default of \code{\link[=homogeneity]{homogeneity()}}).}

\item{axes}{Length-2 integer vector giving the two axes to display.
Default \code{c(1, 2)}.}

\item{...}{Currently ignored.}
}
\value{
A \pkg{ggplot2} object (drawn when printed).
}
\description{
Displays the \strong{space of deviations} (Le Roux, Bienaise & Durand 2019,
Fig. 5.14): the null deviation O (origin, "no difference"), the observed
deviation \eqn{D_{obs} = G_{c_2} - G_{c_1}} between the two group mean points,
and the \eqn{(1-\alpha)} compatibility region: the principal
\eqn{\kappa}-ellipse of the reference cloud, centred on \eqn{D_{obs}}. When O
falls outside the ellipse, the two groups are heterogeneous at level
\eqn{\alpha}.
}
\details{
Requires the \pkg{ggplot2} package and a two- (or more) dimensional analysis.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
grp <- c(1, 1, 3, 3, 3, 1, 3, 3, 2, 2)
plot(homogeneity(Target, grp, groups = c(1, 2)))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=homogeneity]{homogeneity()}}
}
