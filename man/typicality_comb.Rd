% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typicality_comb.R
\name{typicality_comb}
\alias{typicality_comb}
\title{Combinatorial typicality test for a mean point}
\usage{
typicality_comb(
  reference,
  group,
  axes = NULL,
  notable = 0.4,
  alpha = 0.05,
  max_samples = 1e+06,
  seed = NULL,
  keep_perm = FALSE,
  ...
)
}
\arguments{
\item{reference}{Reference cloud: a numeric matrix/data frame of principal
coordinates (one row per point, one column per axis), or a GDA result
object from \pkg{FactoMineR}, \pkg{GDAtools} or \pkg{ade4} (coordinates are
extracted with \code{\link[=get_coord]{get_coord()}}).}

\item{group}{The group cloud whose mean point is compared with the reference
centre. One of:
\itemize{
\item a numeric matrix/data frame of group-point coordinates, expressed in the
same coordinate system (same axes) as \code{reference};
\item a logical vector (length = number of reference points) flagging the
individuals that make up the group;
\item an integer or character vector indexing reference rows.
}}

\item{axes}{Integer vector of axes (columns of the principal coordinates) on
which the test is run, or \code{NULL} (default) for all available axes. Note
that the test is performed in the subspace spanned by \code{axes}; to obtain the
genuine full-cloud test, retain \emph{all} principal axes in your GDA.}

\item{notable}{Notable-limit for the descriptive magnitude of the deviation
(on the Mahalanobis-distance scale). Default \code{0.4} (the book's rule of
thumb).}

\item{alpha}{Level of the compatibility region. Default \code{0.05}.}

\item{max_samples}{Maximum number of samples. Exhaustive enumeration is used
when \eqn{\binom{n}{n_c} \le} \code{max_samples}, Monte Carlo otherwise.
Default \code{1e6}.}

\item{seed}{Optional integer seed, used in the Monte Carlo case for
reproducibility.}

\item{keep_perm}{Logical; if \code{TRUE}, the full vector of permutation
statistics is stored in the result (component \code{perm}). Default \code{FALSE}.}

\item{...}{Passed on to \code{\link[=get_coord]{get_coord()}}.}
}
\value{
An object of class \code{"typicality_comb"} (inheriting
\code{"gdainference_test"}): a list with components including \code{statistic}
(Mahalanobis distance \eqn{D}), \code{statistic2} (\eqn{d^2_{obs}}), \code{p_value},
\code{n_sup}, \code{n_perm}, \code{method}, \code{dim} (dimensionality \eqn{L}), \code{n}, \code{n_c},
\code{notable} and \code{compatibility}. Print it for a formatted summary.
}
\description{
Performs the exact combinatorial (permutation) typicality test of Le Roux,
Bienaise and Durand (2019, chapter 3). It assesses whether the mean point of
a \emph{group} cloud deviates from the mean point (centre) of a \emph{reference} cloud,
using as test statistic the squared Mahalanobis distance \eqn{D^2} between
the two mean points, taken with respect to the covariance structure of the
reference cloud.
}
\details{
The combinatorial distribution of \eqn{D^2} is obtained by considering every
subset of \eqn{n_c} points drawn from the \eqn{n} reference points (where
\eqn{n_c} is the size of the group). When the number of such subsets,
\eqn{\binom{n}{n_c}}, does not exceed \code{max_samples}, the exact exhaustive
distribution is computed; otherwise \code{max_samples} subsets are drawn at random
(Monte Carlo). The (inclusive) p-value is the proportion of subsets whose
statistic is greater than or equal to the observed \eqn{d^2_{obs}}.
}
\examples{
# Target example from Le Roux et al. (2019), chapter 3:
# a group of 4 points compared with a reference cloud of 10 points.
res <- typicality_comb(Target, Target_group)
res
res$p_value   # 9 / 210 = 0.0428...
}
\references{
Le Roux, B., Bienaise, S. & Durand, J.-L. (2019).
\emph{Combinatorial Inference in Geometric Data Analysis}. Chapman & Hall/CRC.
}
\seealso{
\code{\link[=get_coord]{get_coord()}}
}
