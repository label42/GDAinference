% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typicality_byvar.R
\name{typicality_byvar}
\alias{typicality_byvar}
\title{Combinatorial typicality test for every category of a variable}
\usage{
typicality_byvar(
  reference,
  variable,
  axes = NULL,
  notable = 0.4,
  alpha = 0.05,
  max_samples = 1e+06,
  seed = NULL,
  ...
)
}
\arguments{
\item{reference}{Reference cloud: a numeric matrix/data frame of principal
coordinates, or a GDA result object from \pkg{FactoMineR}, \pkg{GDAtools}
or \pkg{ade4} (coordinates are extracted with \code{\link[=get_coord]{get_coord()}}).}

\item{variable}{The grouping variable: a factor / character vector of length
\eqn{n} aligned to the active individuals, or a length-one character giving
the name of a variable stored in \code{reference} (e.g. a \code{quali.sup} variable
of a FactoMineR \code{MCA}).}

\item{axes}{Integer vector of axes on which to run the tests, or \code{NULL}
(default) for all available axes.}

\item{notable, alpha, max_samples, seed}{Passed to \code{\link[=typicality_comb]{typicality_comb()}}.}

\item{...}{Passed on to \code{\link[=get_coord]{get_coord()}}.}
}
\value{
An object of class \code{"typicality_byvar"}: a list with \code{table} (a data
frame with one row per category: \code{category}, \code{n_c}, \code{D}, \code{p_value},
\code{notable}, \code{method}, \code{n_perm}) and \code{results} (the list of the underlying
\code{\link[=typicality_comb]{typicality_comb()}} objects, named by category). Print it for a formatted
table.
}
\description{
Runs \code{\link[=typicality_comb]{typicality_comb()}} for each category of a supplementary / grouping
variable, comparing the subcloud of each category with the whole cloud, and
collects the results in a tidy table. This is the multidimensional, \emph{exact}
counterpart of the per-axis approximation offered by GDAtools'
\code{dimtypicality()}.
}
\examples{
# Illustration on the Target cloud, grouped by the sign of the 2nd axis:
g <- factor(ifelse(Target$Y >= 0, "upper", "lower"))
typicality_byvar(Target, g)

# From a GDA result, naming a supplementary variable directly:
# typicality_byvar(my_mca, "degree")
}
\references{
Le Roux, B., Bienaise, S. & Durand, J.-L. (2019).
\emph{Combinatorial Inference in Geometric Data Analysis}. Chapman & Hall/CRC.
}
\seealso{
\code{\link[=typicality_comb]{typicality_comb()}}
}
