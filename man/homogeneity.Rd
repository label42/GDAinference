% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/homogeneity.R
\name{homogeneity}
\alias{homogeneity}
\title{Combinatorial homogeneity test}
\usage{
homogeneity(
  x,
  group,
  groups = NULL,
  comparison = c("partial", "specific"),
  axes = NULL,
  notable = 0.04,
  alpha = 0.05,
  max_samples = 1e+05,
  seed = NULL,
  n_dir = 500L,
  keep_perm = FALSE,
  keep_geometry = TRUE,
  ...
)
}
\arguments{
\item{x}{The cloud: a numeric matrix/data frame of principal coordinates
(one row per individual, one column per axis), or a GDA result object from
\pkg{FactoMineR}, \pkg{GDAtools} or \pkg{ade4} (coordinates are extracted
with \code{\link[=get_coord]{get_coord()}}).}

\item{group}{The grouping variable that defines the groups to compare: a
factor or vector of length \eqn{n} (aligned to the rows of \code{x}), or the name
of a variable stored in \code{x} (e.g. a supplementary variable of a FactoMineR
analysis). See \code{\link[=typicality_comb]{typicality_comb()}} for the resolution rules.}

\item{groups}{Optional vector naming the categories of \code{group} to compare
(two or more), e.g. \code{groups = c("Right", "Left")}. With two groups the
deviation is oriented from the first to the second (\code{groups[2] - groups[1]}). If \code{NULL} (default) all the categories of \code{group} are compared
(the global comparison).}

\item{comparison}{\code{"partial"} (default) or \code{"specific"}; see Details. The
distinction matters only when a strict subset of the categories of \code{group}
is compared.}

\item{axes}{Integer vector of axes (columns of the principal coordinates) on
which the test is run, or \code{NULL} (default) for all available axes. The test
is performed in the subspace spanned by \code{axes}.}

\item{notable}{Notable-limit for the descriptive magnitude of the difference,
on the \emph{proportion of variance} (partial \eqn{\eta^2}) scale. Default
\code{0.04}, the book's rule of thumb (Le Roux et al. 2019, p. 142).}

\item{alpha}{Level of the compatibility region. Default \code{0.05}.}

\item{max_samples}{Maximum number of arrangements. Exhaustive enumeration is
used when the number of arrangements does not exceed \code{max_samples}, Monte
Carlo otherwise. Default \code{1e5}, as in the reference script of Le Roux et
al. (2019).}

\item{seed}{Optional integer seed, used both for the Monte Carlo sampling and
for the random directions of the compatibility region, for reproducibility.}

\item{n_dir}{Number of random directions used to adjust the compatibility
region in dimension > 1. Default \code{500}. Ignored when the cloud is
one-dimensional (the region is then an exact interval) or when more than
two groups are compared (no region).}

\item{keep_perm}{Logical; if \code{TRUE}, the full vector of permutation
statistics is stored in the result (component \code{perm}). Default \code{FALSE}.}

\item{keep_geometry}{Logical; if \code{TRUE} (default), the coordinates and
summary geometry needed by \code{\link[=plot.homogeneity]{plot.homogeneity()}} are stored in the result
(component \code{geometry}). Set to \code{FALSE} for a lighter object.}

\item{...}{Passed on to \code{\link[=get_coord]{get_coord()}}.}
}
\value{
An object of class \code{"homogeneity"} (inheriting \code{"gdainference_test"});
print it for a formatted summary. Key components: \code{n_groups}, \code{groups},
\code{sizes}, \code{vm} (between-group Mahalanobis variance), \code{pv} (proportion of
variance, partial \eqn{\eta^2}), \code{p_value}, \code{n_sup}, \code{n_perm}, \code{method},
\code{dim}, \code{comparison}, and (for two groups) \code{statistic} (Mahalanobis distance
\eqn{D_M}), \code{statistic2}, \code{sided} and \code{compatibility}.
}
\description{
Performs the exact combinatorial (permutation) homogeneity test of Le Roux,
Bienaise and Durand (2019, chapter 5). It assesses whether several groups of
individuals differ in a geometric cloud, by reallocating the individuals to
the groups in all possible ways (label permutations) while keeping the group
sizes fixed.
}
\details{
The number of groups compared determines the test statistic:
\itemize{
\item \strong{Two groups} (the case of section 5.4): the statistic is the squared
Mahalanobis distance \eqn{D^2_M} between the two group mean points, which
comes with a geometric \strong{compatibility region} and, in one dimension, a
signed one-sided test and an exact compatibility interval.
\item \strong{More than two groups} (the general case of section 5.3): the statistic
is the between-group Mahalanobis variance \eqn{V_M} of the group mean
points (an omnibus test of heterogeneity). No compatibility region is
defined in this case.
}

Two kinds of comparison are available (Le Roux et al. 2019, section 5.3):
\itemize{
\item \strong{partial} (the default): the groups are compared \emph{within} the whole
cloud. All individuals are kept; those outside the groups of interest are
pooled into a residual group, and the Mahalanobis metric is that of the
whole cloud.
\item \strong{specific}: the cloud is restricted to the groups of interest (the metric
is then that of the sub-cloud they form). For two groups this is equivalent
to the combinatorial typicality test (\code{\link[=typicality_comb]{typicality_comb()}}) of one group
against the union of the two (Le Roux et al. 2019, Theorem 5.1).
}

When all the categories of \code{group} are compared there is no residual group
and the two kinds coincide: this is the \strong{global} comparison.

When the number of arrangements does not exceed \code{max_samples}, the exact
exhaustive distribution is computed; otherwise \code{max_samples} arrangements are
drawn at random (Monte Carlo). In the exhaustive case the (inclusive)
p-value is the exact proportion of arrangements whose statistic is greater
than or equal to the observed one; in the Monte Carlo case the add-one
correction of Phipson & Smyth (2010) is applied (\eqn{p = (b + 1)/(B + 1)}),
so the estimated p-value is valid and never zero.

\strong{One-sided convention (two groups on a single axis).} When two groups are
compared in a one-dimensional cloud, the p-value is one-sided \emph{in the
direction of the observed difference} (the book's convention). Because that
direction is chosen from the data, a non-directional claim at level
\eqn{\alpha} requires comparing the one-sided p-value to \eqn{\alpha/2};
this matches the compatibility interval, which excludes zero exactly when
\eqn{p < \alpha/2}.

\strong{Compatibility region and randomness.} In dimension > 1 the two-group
region is \emph{adjusted} over \code{n_dir} random directions (Le Roux et al. 2019,
§5.4.3), so \eqn{\kappa} varies slightly from run to run — even when the
permutation distribution itself is exhaustive — unless \code{seed} is set.
Increase \code{n_dir} to stabilise it.
}
\examples{
# Target example from Le Roux et al. (2019), chapter 5: three groups of the
# 10-point cloud (groups i7, i8 pooled into group 3, giving sizes 3, 2, 5).
grp <- c(1, 1, 3, 3, 3, 1, 3, 3, 2, 2)
homogeneity(Target, grp, groups = c(1, 2))                # two groups: p = 1/2520
homogeneity(Target, grp, groups = c(1, 2), comparison = "specific")  # 2/10
homogeneity(Target, grp)        # global omnibus of the three groups: p = 37/2520
}
\references{
Le Roux, B., Bienaise, S. & Durand, J.-L. (2019).
\emph{Combinatorial Inference in Geometric Data Analysis}. Chapman & Hall/CRC.

Phipson, B. & Smyth, G. K. (2010). Permutation p-values should never be
zero: calculating exact p-values when permutations are randomly drawn.
\emph{Statistical Applications in Genetics and Molecular Biology}, 9(1),
Article 39.
}
\seealso{
\code{\link[=typicality_comb]{typicality_comb()}}, \code{\link[=typicality_geom]{typicality_geom()}}, \code{\link[=get_coord]{get_coord()}}
}
