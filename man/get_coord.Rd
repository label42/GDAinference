% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_coord.R
\name{get_coord}
\alias{get_coord}
\alias{get_coord.default}
\alias{get_coord.PCA}
\alias{get_coord.MCA}
\alias{get_coord.FAMD}
\alias{get_coord.CA}
\alias{get_coord.speMCA}
\alias{get_coord.csMCA}
\alias{get_coord.bcMCA}
\alias{get_coord.wcMCA}
\alias{get_coord.stMCA}
\alias{get_coord.multiMCA}
\alias{get_coord.dudi}
\title{Extract principal coordinates from a GDA result}
\usage{
get_coord(x, axes = NULL, ...)

\method{get_coord}{default}(x, axes = NULL, ...)

\method{get_coord}{PCA}(x, axes = NULL, ...)

\method{get_coord}{MCA}(x, axes = NULL, ...)

\method{get_coord}{FAMD}(x, axes = NULL, ...)

\method{get_coord}{CA}(x, axes = NULL, ...)

\method{get_coord}{speMCA}(x, axes = NULL, ...)

\method{get_coord}{csMCA}(x, axes = NULL, ...)

\method{get_coord}{bcMCA}(x, axes = NULL, ...)

\method{get_coord}{wcMCA}(x, axes = NULL, ...)

\method{get_coord}{stMCA}(x, axes = NULL, ...)

\method{get_coord}{multiMCA}(x, axes = NULL, ...)

\method{get_coord}{dudi}(x, axes = NULL, ...)
}
\arguments{
\item{x}{A GDA result object or a numeric matrix/data frame of coordinates.}

\item{axes}{Integer vector of axes (columns) to keep, or \code{NULL} (default)
to keep all available axes.}

\item{...}{Currently unused; for method extensibility.}
}
\value{
A numeric matrix of principal coordinates with row names preserved
when available.
}
\description{
\code{get_coord()} is the compatibility layer of the package: it returns a plain
numeric matrix of principal coordinates (one row per point, one column per
axis) from the result objects of the most common Geometric Data Analysis
packages, so that the inference tests can be run uniformly whatever the
upstream GDA tool.
}
\details{
Supported inputs:
\itemize{
\item a numeric \code{matrix} or \code{data.frame} of coordinates (returned as is,
after axis selection);
\item \pkg{FactoMineR} results: \code{PCA}, \code{MCA}, \code{FAMD} (individual
coordinates) and \code{CA} (row coordinates);
\item \pkg{GDAtools} results: \code{speMCA}, \code{csMCA}, \code{bcMCA}, \code{wcMCA},
\code{stMCA}, \code{multiMCA} (individual coordinates);
\item \pkg{ade4} results: any \code{dudi} (row coordinates, slot \code{li}).
}
}
\examples{
m <- matrix(rnorm(20), ncol = 2, dimnames = list(NULL, c("Dim1", "Dim2")))
get_coord(m)
get_coord(m, axes = 1)
}
