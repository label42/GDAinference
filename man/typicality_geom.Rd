% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typicality_geom.R
\name{typicality_geom}
\alias{typicality_geom}
\title{Geometric typicality test for a mean point}
\usage{
typicality_geom(
  x,
  point = 0,
  group = NULL,
  level = NULL,
  axes = NULL,
  notable = 0.4,
  alpha = 0.05,
  max_samples = 1e+05,
  seed = NULL,
  n_dir = 500L,
  keep_perm = FALSE,
  keep_geometry = TRUE,
  ...
)
}
\arguments{
\item{x}{The cloud: a numeric matrix/data frame of principal coordinates, or
a GDA result object from \pkg{FactoMineR}, \pkg{GDAtools} or \pkg{ade4}
(coordinates extracted with \code{\link[=get_coord]{get_coord()}}).}

\item{point}{The reference point \code{P}, as a numeric vector of length equal to
the number of \code{axes}, or a single value recycled to all axes. Default \code{0}
(the origin, i.e. the centre of a GDA cloud).}

\item{group, level}{Optional restriction of the test to a subgroup of the
individuals (e.g. one category of a supplementary variable). \code{group} may be
a logical/index vector, or a grouping variable (vector or stored variable
name) together with \code{level}; see \code{\link[=typicality_comb]{typicality_comb()}}. When omitted, the
whole cloud \code{x} is tested.}

\item{axes}{Integer vector of axes to use, or \code{NULL} (default) for all.}

\item{notable}{Notable-limit for the descriptive magnitude of the deviation
(Mahalanobis distance). Default \code{0.4}.}

\item{alpha}{Level of the compatibility region. Default \code{0.05}.}

\item{max_samples}{Maximum number of sign patterns. Exhaustive enumeration is
used when \eqn{2^{n-1} \le} \code{max_samples}, Monte Carlo otherwise.
Default \code{1e5}.}

\item{seed}{Optional integer seed (Monte Carlo, and the random directions of
the compatibility region) for reproducibility.}

\item{n_dir}{Number of random directions used to adjust the compatibility
region in dimension > 1. Default \code{500}. Ignored when the cloud is
one-dimensional (the region is then an exact interval).}

\item{keep_perm, keep_geometry}{Logical; keep the permutation statistics
(\code{perm}) and the geometry needed by \code{\link[=plot.typicality_geom]{plot.typicality_geom()}}. Defaults
\code{FALSE} and \code{TRUE}.}

\item{...}{Passed on to \code{\link[=get_coord]{get_coord()}}.}
}
\value{
An object of class \code{"typicality_geom"} (inheriting
\code{"gdainference_test"}); print it for a summary. Key components: \code{statistic}
(Mahalanobis distance \eqn{D} between the mean point and \code{P}), \code{statistic2}
(the test statistic \eqn{d^2_{obs}}), \code{p_value}, \code{n_sup}, \code{n_perm},
\code{method}, \code{dim}, \code{n}, \code{notable}, \code{sided}, \code{reference_point} and
\code{compatibility}.
}
\description{
Performs the geometric (sign-flip) typicality test of Le Roux, Bienaise and
Durand (2019, chapter 4). It assesses whether the mean point of a cloud
deviates from a fixed \strong{reference point} \code{P} (for example the origin of a
GDA, i.e. the centre of the whole cloud). Unlike \code{\link[=typicality_comb]{typicality_comb()}} (which
compares a group with a reference \emph{cloud}), this test uses the cloud's own
covariance structure and a reflection symmetry about \code{P}.
}
\details{
The reference distribution is obtained by reflecting each point of the cloud
through \code{P}, one at a time: every one of the \eqn{2^{n-1}} distinct sign
patterns yields a "permuted" cloud, and the test statistic is the squared
generalized Mahalanobis distance between the permuted mean point and \code{P}.
When \eqn{2^{n-1} \le} \code{max_samples} the exact distribution is enumerated,
otherwise \code{max_samples} sign patterns are drawn at random (Monte Carlo). The
p-value is the proportion of permutations whose statistic is at least the
observed one; in the one-dimensional case it is one-sided.
}
\examples{
# Target example (book ch. 4): the 10 impact points vs the centre O = (0, 0).
res <- typicality_geom(Target, point = c(0, 0))
res                       # D = 0.964, p = 86/1024 = 0.084 (compatible)

# One-dimensional Student example: mean sleep gain vs 0 (no effect).
typicality_geom(Student, point = 0)
}
\references{
Le Roux, B., Bienaise, S. & Durand, J.-L. (2019).
\emph{Combinatorial Inference in Geometric Data Analysis}. Chapman & Hall/CRC.
}
\seealso{
\code{\link[=typicality_comb]{typicality_comb()}}, \code{\link[=get_coord]{get_coord()}}
}
