% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.typicality_geom}
\alias{plot.typicality_geom}
\title{Plot a geometric typicality test}
\usage{
\method{plot}{typicality_geom}(x, axes = c(1, 2), ...)
}
\arguments{
\item{x}{A \code{typicality_geom} object created with \code{keep_geometry = TRUE}
(the default of \code{\link[=typicality_geom]{typicality_geom()}}).}

\item{axes}{Length-2 integer vector giving the two axes to display.
Default \code{c(1, 2)}.}

\item{...}{Currently ignored.}
}
\value{
A \pkg{ggplot2} object (drawn when printed).
}
\description{
Displays, on a plane of two axes, the cloud, its mean point G, the reference
point P, and the \eqn{(1-\alpha)} compatibility region: the principal
\eqn{\kappa}-ellipse of the cloud, centred on G. When P falls outside the
ellipse, the mean point is atypical of P at level \eqn{\alpha}.
}
\details{
Requires the \pkg{ggplot2} package and a two- (or more) dimensional analysis.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
plot(typicality_geom(Target, point = c(0, 0)))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=typicality_geom]{typicality_geom()}}
}
