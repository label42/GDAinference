# Print a geometric typicality test

Print a geometric typicality test

## Usage

``` r
# S3 method for class 'typicality_geom'
print(x, digits = 4, ...)
```

## Arguments

- x:

  A `typicality_geom` object (from
  [`typicality_geom()`](https://label42.github.io/GDAinference/reference/typicality_geom.md)).

- digits:

  Number of significant digits for the statistics.

- ...:

  Ignored.

## Value

`x`, invisibly.
