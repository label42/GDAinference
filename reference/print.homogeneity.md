# Print a combinatorial homogeneity test

Print a combinatorial homogeneity test

## Usage

``` r
# S3 method for class 'homogeneity'
print(x, digits = 4, ...)
```

## Arguments

- x:

  A `homogeneity` object (from
  [`homogeneity()`](https://label42.github.io/GDAinference/reference/homogeneity.md)).

- digits:

  Number of significant digits for the statistics.

- ...:

  Ignored.

## Value

`x`, invisibly.
