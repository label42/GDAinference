# Print a by-category typicality table

Print a by-category typicality table

## Usage

``` r
# S3 method for class 'typicality_byvar'
print(x, digits = 3, ...)
```

## Arguments

- x:

  A `typicality_byvar` object.

- digits:

  Number of decimal places for `D` and the p-values.

- ...:

  Ignored.

## Value

`x`, invisibly.
